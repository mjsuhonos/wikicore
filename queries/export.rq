PREFIX : <http://example.org/ontology/>

SELECT ?entity
WHERE {
  # --- use precomputed ancestors ---
  ?entity :ancestor1 ?a1 .
  
  # --- require sitelink presence ---
  ?entity :hasSitelink "true" .
}
GROUP BY ?entity
